// Package plugins registers all built-in and third-party plugin factories.
// Add your plugin's Register() call below.
package plugins

// RegisterAll registers every plugin factory. Call this once from main().
func RegisterAll() {
	//audit.Register()
}
