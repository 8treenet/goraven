# Docling document converter dependencies
# Install: pip install -r requirements.txt

# Core library
docling>=2.115.0

# GPU acceleration (optional - auto-detected by docling)
# torch>=2.0  # Uncomment if GPU acceleration needed
